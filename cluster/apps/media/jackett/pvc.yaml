---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: jackett-config
  namespace: media
spec:
  storageClassName: jackett-config
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${SECRET_NFS_SERVER}
    path: "${SECRET_NFS_ROOTDIR}/media/jackett"
  mountOptions:
    - nfsvers=4
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
    - nolock
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jackett-config
  namespace: media
  labels:
    app.kubernetes.io/name: &name jackett
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  storageClassName: jackett-config
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
