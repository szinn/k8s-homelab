---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
  namespace: dbms
stringData:
  postgres-password: ${POSTGRES_ROOT_PASSWORD}
  password: ${POSTGRES_ROOT_PASSWORD}
  replication-password: ${POSTGRES_ROOT_PASSWORD}
