---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-superuser
  namespace: dbms
stringData:
  username: postgres
  password: ${POSTGRES_ROOT_PASSWORD}
  MINIO_ACCESS_KEY: postgresql
  MINIO_SECRET_KEY: ${POSTGRESQL_S3_PASSWORD}
  MINIO_REGION: ca-wat-1
