---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: dashy-nfs-pv
  namespace: dashboard
  labels:
    app.kubernetes.io/name: &name dashy
    app.kubernetes.io/instance: *name
spec:
  storageClassName: dashy-nfs
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${SECRET_NFS_SERVER}
    path: "${SECRET_NFS_ROOTDIR}/dashboard/dashy"
  mountOptions:
    - nfsvers=4
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
    - nolock
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashy-pvc
  namespace: dashboard
spec:
  storageClassName: dashy-nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
