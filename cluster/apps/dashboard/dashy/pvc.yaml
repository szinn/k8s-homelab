---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: dashy-config
  namespace: dashboard
  labels:
    app.kubernetes.io/name: &name dashy
    app.kubernetes.io/instance: *name
spec:
  storageClassName: dashy-config
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${SECRET_ATLAS_NFS_SERVER}
    path: "${SECRET_ATLAS_NFS_ROOTDIR}/dashboard/dashy"
  mountOptions:
    - nfsvers=4
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
    - nolock
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashy-config
  namespace: dashboard
  labels:
    app.kubernetes.io/name: &name dashy
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  storageClassName: dashy-config
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
