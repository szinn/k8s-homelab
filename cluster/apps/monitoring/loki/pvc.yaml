---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: monitoring-loki-pvc
  namespace: monitoring
  labels:
    kasten.io/backup-volume: "enabled"
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
