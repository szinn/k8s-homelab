---
apiVersion: v1
kind: Service
metadata:
  name: &app pluto
  namespace: envoy-system
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  type: ExternalName
  externalName: pluto.${SECRET_DOMAIN_NAME}.
  ports:
    - name: pluto
      port: 5000
      protocol: TCP
      targetPort: 5000
