---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: shelfmark
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      shelfmark:
        annotations:
          secret.reloader.stakater.com/reload: shelfmark-secret,shelfmark-gluetun-secret
          config.reloader.stakater.com/reload: shelfmark-coredns

        pod:
          securityContext:
            fsGroup: 2000
            fsGroupChangePolicy: "OnRootMismatch"

        initContainers:
          coredns:
            image:
              repository: mirror.gcr.io/coredns/coredns
              tag: 1.14.7@sha256:7efd3c635b03efd68c4e8398fc45f0d993d0e9ab016f72c1cefb0fd6d01aa286
            args:
              - -conf
              - /etc/coredns/Corefile
            restartPolicy: Always

          gluetun:
            dependsOn:
              - coredns
            image:
              repository: ghcr.io/qdm12/gluetun
              tag: v3.41.3@sha256:fa19cc76b2af13d57a8d3dc3066f2ada061b1c761b8aecf989b3877c0486e027
            env:
              DOT: "off"
              DNS_ADDRESS: "127.0.0.2"
              HTTP_CONTROL_SERVER_AUTH_CONFIG_FILEPATH: &gluetunAuthPath /gluetun/auth.toml
              VPN_SERVICE_PROVIDER: custom
              VPN_TYPE: openvpn
              OPENVPN_CUSTOM_CONFIG: &gluetunOpenvpnPath /gluetun/custom.conf
              VPN_INTERFACE: tun0
              # VPN_SERVICE_PROVIDER: private internet access
              # SERVER_REGIONS: "US Chicago"
              # VPN_PORT_FORWARDING: on
              # VPN_PORT_FORWARDING_PROVIDER: private internet access
              FIREWALL_INPUT_PORTS: 8084
              FIREWALL_OUTBOUND_SUBNETS: 10.200.0.0/16,10.201.0.0/16 # Allow access to k8s subnets
            envFrom:
              - secretRef:
                  name: shelfmark-secret
            restartPolicy: Always
            securityContext:
              capabilities:
                add:
                  - NET_ADMIN
              allowPrivilegeEscalation: false

        containers:
          shelfmark:
            image:
              repository: ghcr.io/calibrain/shelfmark
              tag: v1.3.14@sha256:e19458e598adbb7e4dae422d1cc8fb0d6fda6f532cd97ea2cb6d4e6b04e7e9ca
            command:
              - gunicorn
            args:
              # - --log-level=debug
              - --access-logfile=-
              - --error-logfile=-
              - --worker-class=geventwebsocket.gunicorn.workers.GeventWebSocketWorker
              - --workers=1
              - -t
              - "300"
              - -b
              - 0.0.0.0:8084
              - shelfmark.main:app
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/health
                    port: &port 8084
              readiness: *probe
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 512Mi
            securityContext:
              runAsUser: 2000
              runAsGroup: 2000
              runAsNonRoot: true
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
    persistence:
      config:
        existingClaim: shelfmark-config
      nfs-nas-media:
        type: nfs
        server: ragnar.zinn.tech
        path: /mnt/atlas/media
        advancedMounts:
          shelfmark:
            shelfmark:
              - path: /bookdrop
                subPath: BookLibrary/Bookdrop
      tmpfs:
        type: emptyDir
        sizeLimit: 1Gi
        globalMounts:
          - path: /tmp
            subPath: tmp
          - path: /var/log
            subPath: var-log

      gluetun-auth:
        type: secret
        name: shelfmark-gluetun-secret
        advancedMounts:
          shelfmark:
            gluetun:
              - path: *gluetunAuthPath
                subPath: auth.toml

      gluetun-openvpn:
        type: secret
        name: shelfmark-secret
        advancedMounts:
          shelfmark:
            gluetun:
              - path: *gluetunOpenvpnPath
                subPath: openvpn_config

      coredns:
        type: configMap
        name: shelfmark-coredns
        advancedMounts:
          shelfmark:
            coredns:
              - path: /etc/coredns/Corefile
                subPath: Corefile
                readOnly: true
    route:
      shelfmark:
        hostnames:
          - "{{ .Release.Name }}.zinn.ca"
        parentRefs:
          - name: envoy-internal
            namespace: network
    service:
      shelfmark:
        ports:
          http:
            port: *port
