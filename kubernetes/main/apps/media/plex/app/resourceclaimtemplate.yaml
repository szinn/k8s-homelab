---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/resource.k8s.io/v1beta1/resourceclaimtemplate.json
apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: plex
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: gpu.intel.com
