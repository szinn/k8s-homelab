apiVersion: jaegertracing.io/v1
kind: Jaeger
metadata:
  name: production
# spec:
#   strategy: production
#   collector:
#     maxReplicas: 3
#     resources:
#       limits:
#         cpu: 100m
#         memory: 128Mi
#   storage:
#     type: elasticsearch
#     options:
#       es:
#         server-urls: http://elasticsearch.dbms:9200
#         username: elastic
#         password: ${SECRET_ELASTICSEARCH_PASSWORD}
#         index-prefix: jaeger_
#         version: 7                                  # Necessary as it doesn't work with 8 yet
#         create-index-templates: false               # Necessary as it doesn't work with 8 yet
