---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - cronjob.yaml
  - plex-meta-manager-secrets.yaml
configMapGenerator:
  - name: plex-meta-manager-cm
    files:
      - config.yaml
generatorOptions:
  disableNameSuffixHash: true
