---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-meta-manager-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
