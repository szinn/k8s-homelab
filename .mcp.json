{
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp",
      "headers": {
        "Authorization": "Bearer ${CLAUDE_GITHUB_PAT}"
      }
    },
    "mermaid": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@probelabs/maid-mcp"],
      "env": {}
    }
  }
}
