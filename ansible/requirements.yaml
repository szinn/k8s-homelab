---
# ansible-galaxy collection install -r requirements.yaml
collections:
  - name: ansible.posix
    version: 1.6.2
  - name: community.general
    version: 10.0.0
  - name: community.sops
    version: 2.0.0
